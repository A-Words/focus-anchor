package com.focusanchor.core.model

enum class SuspendItemType(val label: String) {
    Message(label = "回消息"),
    Research(label = "查资料"),
    Idea(label = "灵感"),
    Todo(label = "待办"),
    Purchase(label = "购买"),
    Other(label = "其他"),
}

